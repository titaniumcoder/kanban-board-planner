FROM elixir:1.18

RUN apt-get update -qq && \
    apt-get install -y -qq python3 python3-pip git && \
    apt-get clean && \
    rm -rf /var/lib/apt/lists/* && \
    pip3 install --no-cache-dir aider-chat

RUN useradd --create-home --uid 1000 aider
USER aider
WORKDIR /app
